{
  "name": "stormlogic",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Weather concept app powered by the Open-Meteo API",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:summary && npm run build && npm run test:smoke && npm run test:contrast && npm run test:persistence && npm run test:radar",
    "test:summary": "node scripts/summary-test.mjs",
    "test:smoke": "node scripts/smoke-test.mjs",
    "test:contrast": "node scripts/contrast-check.mjs",
    "test:persistence": "node scripts/persistence-test.mjs",
    "test:radar": "node scripts/radar-test.mjs"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.5",
    "playwright": "^1.62.1",
    "pngjs": "^7.0.0",
    "vite": "^8.2.1"
  }
}
